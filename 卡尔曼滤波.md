> 来源: Gemini 对话记录《L1与L2正则化数学推导与面试解析》（2026/9/24）中关于卡尔曼滤波的部分；证明部分做了补充展开

# 卡尔曼滤波 (Kalman Filter) — 笔记

## 一、它是什么

**卡尔曼滤波**是一种**递归的最优状态估计算法**：从一串带随机噪声的观测数据中，估计动态系统的真实**隐藏状态**。

- 本质上是一个**状态空间连续、服从高斯分布的隐马尔可夫模型 (HMM)**。
- 每一步都在融合两个高斯分布：
  - **预测分布（先验）**：由系统动力学模型推出来；
  - **观测分布（似然）**：由传感器数据给出。
- 对两者求**最大后验估计 (MAP)**，得到一个**方差更小**、更接近真实值的新分布。
- 名字叫“滤波”，本质却是**最优状态估计**。

> 一句话：用已知的物理模型 ($A, B$) 去平滑带噪声的传感器数据，从而得到对系统状态最准确的估计。

## 二、核心思想：蒙眼开车

你蒙着眼开一辆车，想知道每一秒车的位置和速度。手上有两个**都不太靠谱**的信息源：

| 信息源 | 对应 | 缺点 |
|---|---|---|
| **物理规律“盲猜”**：根据上一秒的位置、速度和踩的油门推算 | $A$, $B$, $u_k$ | 路面坑洼、刮风 → **过程噪声 $Q$**；盲猜越久越没底（方差越来越大） |
| **传感器测量**：GPS 每秒报一次位置 | $H$, $z_k$ | GPS 会漂移 → **测量噪声 $R$**；读数在真实值附近随机跳 |

每个时间步，算法都像在跟两个朋友对话：

1. **预测**：“根据刚才的速度，我猜现在在 100 米处”（误差约 ±5 米）。
2. **测量**：GPS 报告“你在 102 米处”（误差约 ±2 米）。
3. **更新**：该信谁？
   - 风大、路况差（$Q$ 大）→ 盲猜不可靠 → 多信 GPS；
   - 开进高楼之间、GPS 乱跳（$R$ 大）→ 多信盲猜。

卡尔曼滤波在数学上严格给出了“该信谁、信多少”的比例，也就是**卡尔曼增益 $K$**，用它把 100 米和 102 米融合成一个最接近真实值的位置。

> 按“±”为标准差算：$P=25,\ R=4$，$K=\frac{25}{29}\approx 0.86$，融合结果 $\approx 100 + 0.86\times 2 \approx 101.7$ 米，更靠近误差更小的 GPS。

## 三、模型定义

卡尔曼滤波假设系统是**线性动力学系统**，用两条方程来描述。

### 1. 状态方程（马尔可夫转移）

$$x_k = A x_{k-1} + B u_k + w_{k-1}, \qquad w_{k-1} \sim \mathcal{N}(0, Q)$$

| 符号 | 含义 |
|---|---|
| $x_k$ | $k$ 时刻系统的**真实状态**（隐藏，比如 [位置, 速度]） |
| $A$ | **状态转移矩阵**：系统的惯性规律（相当于把常微分方程离散化后往前推一步） |
| $u_k$ | **控制输入**：$k$ 时刻主动发出的指令，比如“油门踩下 30%” |
| $B$ | **控制输入矩阵**：把控制指令换算成对状态的影响（踩 30% 油门 → 加速度增加多少） |
| $w_{k-1}$ | **过程噪声**：建模的不确定性（坑洼、侧风等写不进公式的扰动） |

- **$A x_{k-1}$** 是松开油门、靠惯性滑行时的演化；**$B u_k$** 是你主动施加的干预。两者相加才是合理的物理盲猜。踩了油门却不加 $B u_k$，预测就会错得离谱。
- **$A$ 和 $B$ 不是训练出来的**：经典卡尔曼滤波里，它们由工程师根据物理定律（如牛顿定律）推导后直接写死。（系统辨识、强化学习里确实会从数据中学动力学，但那不属于经典设定。）

**例：匀加速小车，$\Delta t = 1$ 秒**

$$
\begin{bmatrix} \text{位置} \\ \text{速度} \end{bmatrix}_k
=
\underbrace{\begin{bmatrix} 1 & 1 \\ 0 & 1 \end{bmatrix}}_{A}
\begin{bmatrix} \text{位置} \\ \text{速度} \end{bmatrix}_{k-1}
+
\underbrace{\begin{bmatrix} 0 \\ 1 \end{bmatrix}}_{B}
\cdot \text{加速度}
$$

即：当前位置 = 上一秒位置 + 上一秒速度 × 1；当前速度 = 上一秒速度 + 加速度 × 1。

### 2. 观测方程

$$z_k = H x_k + v_k, \qquad v_k \sim \mathcal{N}(0, R)$$

| 符号 | 含义 |
|---|---|
| $z_k$ | $k$ 时刻传感器的**观测值** |
| $H$ | **观测矩阵**：把状态空间映射到观测空间（比如状态是 [位置, 速度]，GPS 只测位置，则 $H = [1\ \ 0]$） |
| $v_k$ | **测量噪声**：传感器固有误差，$R$ 通常是出厂参数 |

另外默认 $w$、$v$ 互相独立，并且与初始状态独立。

## 四、算法：预测 → 更新 的 5 条公式

算法是递归的，只需要保存上一时刻的 $\hat{x}_{k-1}$ 和 $P_{k-1}$，所以**内存复杂度是 $O(1)$**，很适合实时计算。上标 $^-$ 表示还没用观测修正过的**先验**（“半成品”）。

### 第一阶段：预测（Predict）—— 按物理规律盲猜

**① 状态预测**

$$\hat{x}_k^- = A \hat{x}_{k-1} + B u_k$$

**② 协方差预测**

$$P_k^- = A P_{k-1} A^\top + Q$$

加上 $Q$ 以后，不确定性在这一步**变大**。

### 第二阶段：更新（Update）—— 融合传感器数据

**③ 卡尔曼增益（算法的核心）**

$$K_k = P_k^- H^\top \left(H P_k^- H^\top + R\right)^{-1}$$

把预测误差 $P_k^-$ 和测量误差 $R$ 放在一起比较，误差大的一方分到的权重小。

**④ 状态更新**

$$\hat{x}_k = \hat{x}_k^- + K_k \left(z_k - H \hat{x}_k^-\right)$$

$z_k - H\hat{x}_k^-$（实际观测减去预期观测）叫做**残差 / 新息 (Innovation)**。

**⑤ 协方差更新**

$$P_k = (I - K_k H) P_k^-$$

同时参考了模型和观测（兼听则明），不确定性**缩小**。

然后带着 $\hat{x}_k$ 和 $P_k$ 回到 ①，开始下一个时间步。

```
        ┌──────────── 预测 ────────────┐      ┌───────────── 更新 ─────────────┐
x̂_{k-1}, P_{k-1} ─► x̂_k⁻ = A x̂_{k-1} + B u_k ─► K_k ─► x̂_k = x̂_k⁻ + K_k(z_k − H x̂_k⁻) ─► x̂_k, P_k
                    P_k⁻ = A P_{k-1} Aᵀ + Q        ▲    P_k = (I − K_k H) P_k⁻               │
                                                  z_k                                     │
        ▲─────────────────────────────── 下一个时间步 ◄──────────────────────────────────┘
```

### 两个阶段为什么缺一不可

- **状态方程提供加权平均的“锚”**：GPS 再怎么跳，也需要一个相对稳定的基准把它拉住。$A x_{k-1} + B u_k$ 就是这个锚，它的结果就是更新阶段的 $\hat{x}^-$。只有一个乱跳的传感器读数，没法做加权平均，也就谈不上滤波。
- **$w_{k-1}$ 决定要让步多少**：每往前推一步，$Q$ 都会让 $P^-$ 变大。路况越差，$Q$ 越大，$P^-$ 越大，于是 $K$ 越大，权重就越偏向传感器。

## 五、为什么能“滤波”：本质是动态加权平均

“滤波”原本是信号处理里的词：**从带杂音、带干扰的数据里提取干净的真实信号**，可以把它想成一个“数据净水器”。它滤掉的是两类噪声：

1. **测量噪声**：GPS 读数这一秒多报 3 米、下一秒少报 2 米。引入物理预测后，这些毛刺被磨平，得到平滑的轨迹。
2. **过程噪声**：坑洼、侧风让纯物理推算越推越偏。不断引入 GPS 数据，把累积的偏差修正回来。

### 一维标量情形

令 $A=H=1$，预测方差为 $P$，测量方差为 $R$。

**状态更新就是加权平均**：

$$\hat{x}_{\text{最终}} = \hat{x}_{\text{预测}} + K(z_{\text{测量}} - \hat{x}_{\text{预测}}) = (1-K)\,\hat{x}_{\text{预测}} + K\, z_{\text{测量}}$$

- $K$ 是传感器读数的权重，$1-K$ 是模型预测的权重。

**增益由两边的方差决定**：

$$K = \frac{P}{P + R}$$

| 极端情况 | $K$ | 结果 | 滤波效果 |
|---|---|---|---|
| 传感器极差，$R \to \infty$ | $K \to 0$ | $\hat{x} \approx \hat{x}_{\text{预测}}$ | 传感器数据被完全屏蔽，只信物理推算 |
| 传感器极准，$R \to 0$ | $K \to 1$ | $\hat{x} \approx z_{\text{测量}}$ | 丢掉盲猜，完全采用传感器读数 |

**本质**：每一步都用 $K = \frac{P}{P+R}$ 实时比较两边的噪声水平，哪边噪声大就把哪边的权重调小。剧烈跳动的随机噪声被乘上一个很小的系数，在最终估计里被熨平。

## 六、推导与证明

### 1. 协方差预测 $P_k^- = A P_{k-1} A^\top + Q$

定义误差 $e_{k-1} = x_{k-1} - \hat{x}_{k-1}$，$e_k^- = x_k - \hat{x}_k^-$。两条方程相减，$B u_k$ 是已知量，被消掉：

$$e_k^- = A e_{k-1} + w_{k-1}$$

$w_{k-1}$ 与 $e_{k-1}$ 独立，所以交叉项为 0：

$$P_k^- = \mathbb{E}[e_k^- e_k^{-\top}] = A P_{k-1} A^\top + Q \qquad \blacksquare$$

### 2. 卡尔曼增益：三种统计思想，得到同一个答案

#### (a) 最小均方误差估计 (MMSE) —— 最直接的推导

目标是让估计值与真实值的误差平方期望最小。在多维情形下，这等价于让后验协方差 $P_k$ 的**迹**（各状态分量方差之和）最小。

更新后的误差为

$$e_k = x_k - \hat{x}_k = x_k - \hat{x}_k^- - K(Hx_k + v_k - H\hat{x}_k^-) = (I - KH)\,e_k^- - K v_k$$

$e_k^-$ 与 $v_k$ 独立，于是（这是对任意 $K$ 都成立的 **Joseph 形式**）：

$$P_k = (I-KH)\,P_k^-\,(I-KH)^\top + K R K^\top$$

展开，并记 $S = H P_k^- H^\top + R$（新息协方差）：

$$P_k = P_k^- - K H P_k^- - P_k^- H^\top K^\top + K S K^\top$$

对迹关于 $K$ 求导（用到 $\frac{\partial}{\partial K}\mathrm{tr}(KA) = A^\top$，以及 $S$ 对称时 $\frac{\partial}{\partial K}\mathrm{tr}(KSK^\top) = 2KS$）：

$$\frac{\partial\, \mathrm{tr}(P_k)}{\partial K} = -2\,P_k^- H^\top + 2\,K S = 0$$

$$\boxed{K_k = P_k^- H^\top S^{-1} = P_k^- H^\top (H P_k^- H^\top + R)^{-1}}$$

$S$ 正定，所以这是极小值。不论噪声取什么具体值，这个增益给出的都是方差最小的线性估计。

**顺便推出公式 ⑤**：把 $K S = P_k^- H^\top$ 代回，有 $K S K^\top = P_k^- H^\top K^\top$，与展开式里的一项相消：

$$P_k = P_k^- - K H P_k^- = (I - K_k H) P_k^- \qquad \blacksquare$$

（这个简化形式只在 $K$ 取最优值时成立；Joseph 形式对任意 $K$ 都成立，数值上也更稳定。）

#### (b) 正交原理 (Orthogonality Principle)

线性最优估计的误差必须和用来估计的观测数据**正交**（统计上不相关）。否则误差里还残留着观测中的信息，说明数据没用完。

要求 $e_k$ 与新息 $\tilde{y}_k = z_k - H\hat{x}_k^- = H e_k^- + v_k$ 不相关：

$$\mathbb{E}[e_k \tilde{y}_k^\top] = \mathbb{E}\big[((I-KH)e_k^- - Kv_k)(He_k^- + v_k)^\top\big] = (I-KH)P_k^-H^\top - KR = 0$$

$$\Rightarrow\ K(HP_k^-H^\top + R) = P_k^-H^\top \ \Rightarrow\ K_k = P_k^-H^\top(HP_k^-H^\top + R)^{-1} \qquad \blacksquare$$

#### (c) 贝叶斯推断 / 最大后验估计 (MAP)

卡尔曼滤波就是**高斯分布下的贝叶斯推断**：

- 先验：$x_k \sim \mathcal{N}(\hat{x}_k^-, P_k^-)$（物理模型的预测）
- 似然：$z_k \mid x_k \sim \mathcal{N}(Hx_k, R)$（传感器测量）
- 后验 ∝ 先验 × 似然，仍是高斯分布，其均值就是 MAP 解。

**一维直观**：两个高斯相乘，

$$\mathcal{N}(\hat{x}^-, P)\cdot\mathcal{N}(z, R) \ \propto\ \mathcal{N}\!\left(\frac{R\,\hat{x}^- + P\,z}{P+R},\ \frac{PR}{P+R}\right)$$

- 均值 $= \hat{x}^- + \frac{P}{P+R}(z - \hat{x}^-)$，正好是 $K = \frac{P}{P+R}$；
- 方差 $\frac{PR}{P+R} = (1-K)P$，比 $P$ 和 $R$ 都小，对应公式 ⑤。
- 均值的权重由**精度**（方差的倒数）决定：$\frac{1/P}{1/P+1/R}$ 与 $\frac{1/R}{1/P+1/R}$，谁的方差小就偏向谁。

**多维情形**：MAP 等价于最小化

$$J(x) = (x-\hat{x}_k^-)^\top (P_k^-)^{-1}(x-\hat{x}_k^-) + (z_k - Hx)^\top R^{-1}(z_k - Hx)$$

令梯度为 0，得到信息形式 $P_k^{-1} = (P_k^-)^{-1} + H^\top R^{-1} H$。再用矩阵求逆引理 (Woodbury) 化简，结果与上面的 $K_k$ 和公式 ⑤ 相同。

### 3. 增益公式的矩阵映射直观

状态空间（如 [位置, 速度]）和观测空间（如只有位置）的维度往往不同，所以需要 $H$ 在两者之间转换：

| 部分 | 含义 |
|---|---|
| $H P_k^- H^\top$ | 把模型的预测方差**投影到测量空间** |
| $(H P_k^- H^\top + R)^{-1}$ | 在测量空间里，预测与测量的**总协方差的逆**（总精度） |
| $P_k^- H^\top$ | 把测量空间里算好的比例**映射回状态空间**，分配到各个状态分量上 |

## 七、输入 / 输出

| | 内容 | 说明 |
|---|---|---|
| **每步输入** | 控制指令 $u_k$ | 比如“方向盘右转 5°”“油门 20%” |
| | 传感器读数 $z_k$ | GPS 坐标、激光雷达距离等，带噪声 |
| **初始化配置** | $A, B, H, Q, R$，初值 $\hat{x}_0, P_0$ | 物理规律和硬件误差参数，写死，不需要每步输入 |
| **每步输出** | 最优状态估计 $\hat{x}_k$ | 滤掉噪声后的“真相”：位置、速度、朝向…… |
| | 误差协方差 $P_k$ | 对这个估计有多大把握，越小越可信；下游控制可以据此决策（比如是否紧急刹车） |

## 八、应用：传感器融合 (Sensor Fusion)

没有哪个传感器是完美的。卡尔曼滤波在工业界最大的用途，就是把一堆各有缺陷的传感器组合成一个可靠的系统。

- **自动驾驶 / 无人机**：GPS 会漂移，雷达有噪点，IMU 的误差会随时间累积。三者融合后输出平滑、精确的轨迹。
- **航天器 / 导弹导航**：阿波罗登月计划是最早的著名应用。太空中没有 GPS，飞船靠内部陀螺仪推算，再配合地面的无线电测距，两者实时融合以保证精准变轨。
- **扫地机器人 (SLAM)**：轮子在光滑地板上打滑，导致里程推算出错；激光雷达有盲区。融合两者后才能画出准确的户型图。
- **金融高频交易**：股票的内在价值是隐藏状态，把公开报价和成交量当作带噪声的“传感器读数”，估计价值的真实走势。

## 九、局限与延伸

- 标准卡尔曼滤波**严格要求线性系统和高斯噪声**。
- 遇到高度非线性的动力学时，可以：
  - 用**泰勒展开做局部线性化**，即**扩展卡尔曼滤波 (EKF)**：把 $A$、$H$ 换成非线性函数在当前估计点的雅可比矩阵；
  - 用**数值采样方法**，如 **无迹卡尔曼滤波 (UKF)** 用确定性的 sigma 点传播分布，**粒子滤波**用随机采样近似任意分布。

## 十、面试速记

- **定义**：线性高斯状态空间模型上的递归贝叶斯估计；是连续状态的 HMM。
- **两步 5 条公式**：预测（$\hat{x}^-$、$P^-$，不确定性增大）→ 更新（$K$、$\hat{x}$、$P$，不确定性减小）。
- **$K$ 的直觉**：$K = \frac{P}{P+R}$。$R$ 大就信模型，$P$ 大就信传感器。
- **$K$ 的来源**：MMSE（最小化 $\mathrm{tr}(P_k)$）、正交原理（误差与新息不相关）、高斯 MAP（按精度加权）。三者推出的结果相同。
- **为什么叫滤波**：更新式就是 $(1-K)\hat{x}^- + Kz$ 这个动态加权平均，噪声被乘上小权重后被平滑掉。
- **$A, B$ 从哪来**：由物理定律推导，不是学出来的。
- **复杂度**：递归，只存上一步的状态，内存 $O(1)$。
- **非线性情形**：EKF（泰勒 / 雅可比线性化）、UKF、粒子滤波。
